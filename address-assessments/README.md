# Address assessments

Reports produced by the `nyc-address-assessment` Claude Code skill
(`.claude/skills/nyc-address-assessment/`). Each candidate address gets its
own file here; `SUMMARY.md` (created on first assessment) tracks all of them
side by side for comparison.

To assess a new address, just paste it (or a listing link) in a Claude Code
session in this repo.
